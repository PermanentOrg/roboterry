{
	"mcpServers": {
		"db": {
			"type": "stdio",
			"command": "bash",
			"args": [
				"-c",
				"source ./.env && npx dbhub --dsn postgres://prodreadonly:$DATABASE_PASSWORD@$DATABASE_HOST:5432/permanent"
			]
		}
	}
}
